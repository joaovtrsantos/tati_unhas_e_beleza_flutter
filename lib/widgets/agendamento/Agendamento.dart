import 'package:flutter/material.dart';

class Agendamento extends StatelessWidget {
  const Agendamento({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Estamos chegando lá');
  }
}